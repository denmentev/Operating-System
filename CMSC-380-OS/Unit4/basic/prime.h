/* This syntax prevents a file from being included/defined twice */

#ifndef PRIME_H
#define PRIME_H

int is_prime(int n); // return 1 if int is prime, else 0
int get_prime(int n); // returns a random prime less than n

#endif
